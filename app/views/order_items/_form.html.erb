<%= simple_form_for(@order_item) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :quantity %>
    <%= f.association :order %>
    <%= f.association :stock_type %>
    <%= f.association :stock %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
