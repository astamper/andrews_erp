<%= simple_form_for(@unit) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :quantity %>
    <%= f.association :base_unit %>


  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
